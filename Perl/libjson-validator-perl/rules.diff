--- libjson-validator-perl.debianize/debian/rules	2026-06-10 15:13:40.442760566 +0530
+++ libjson-validator-perl/debian/rules	2026-06-10 15:13:56.873087443 +0530
@@ -1,3 +1,10 @@
 #!/usr/bin/make -f
+
+SKIP_TESTS = $(shell cat debian/tests/pkg-perl/smoke-skip)
+TEST_FILES = $(filter-out $(SKIP_TESTS), $(wildcard t/*.t))
+
 %:
-	dh $@ --buildsystem=makefile
+	dh $@
+
+override_dh_auto_test:
+	RES_OPTIONS=attempts:0 http_proxy= dh_auto_test -- TEST_FILES="$(TEST_FILES)"
